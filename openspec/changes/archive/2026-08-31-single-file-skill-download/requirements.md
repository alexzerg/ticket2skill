# Requirements

- Make one self-contained Markdown file the primary skill download.
- Include all runtime instructions, exceptions, constraints, patch, criteria, and provenance.
- Keep the multi-file ZIP only as an optional evidence bundle.
- Keep Firestore as publication evidence.
