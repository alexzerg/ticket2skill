# Test plan

- Ruff, mypy, pytest, JavaScript, and diff checks pass.
- Renderer produces all runtime sections and exact exceptions.
- Primary endpoint returns text/markdown with a Markdown filename.
- Optional ZIP retains lineage and replay evidence.
- Deployed UI presents Markdown as primary without desktop/mobile overflow.
