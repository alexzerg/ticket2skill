# Requirements

- Route a typed controller name using the published temporal skill.
- Exact case-sensitive allowlist matches use the scoped VM exception route.
- Every other controller uses the current GKE/JCasC/GitOps workflow.
- Expose the result interactively after v4 publication and document reproducible checks.
