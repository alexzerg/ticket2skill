# Research

The published TemporalSkill already contains both the exact legacy allowlist and the current workflow. Routing therefore belongs in a pure backend decision function; the frontend must only submit a controller identifier and display the typed result.
