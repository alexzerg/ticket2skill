# Plan

1. Add request/decision models and a pure router.
2. Add POST /api/route-incident using the published skill in state.
3. Add the interactive routing lab to the generated-skill result.
4. Add deterministic routing tests, browser checks, docs, version bump, and deployment.
