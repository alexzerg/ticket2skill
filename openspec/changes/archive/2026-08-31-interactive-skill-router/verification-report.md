# Verification report

The router reads the generated TemporalSkill from application state. Matching is exact and case-sensitive. Legacy responses contain only the matched controller's retained VM actions; current responses contain the generated GKE/JCasC/GitOps workflow. UI output escapes returned text and is responsive on desktop and mobile.
