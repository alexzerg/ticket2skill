# Verification report

The standalone current incident was removed from the initial screen. A concise test incident now appears only inside generated results, immediately before the stale/current comparison. The global stale badge collision was removed by scoping it to `.comparison .stale`; both drift version cards remain rectangular and equal-height.
