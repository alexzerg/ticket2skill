# Plan

1. Add a typed legacy-exception schema and canonical three-controller dataset.
2. Require timeline, v4, and v5 generation to retain the exact allowlist.
3. Extend replay with exact-set and bounded-scope checks.
4. Return and render the policy in the staged demo.
5. Scope CSS selectors and improve timeline/drift responsive layout.
6. Update artifacts, docs, versions, tests, deploy, and verify the live flow.
