# Requirements

- Fix the architecture timeline CSS collision and improve drift-comparison readability.
- Preserve exactly three VM-era Jenkins exceptions: `jenkins-paris`, `jenkins-barcelona`, and `jenkins-NYC`.
- Permit SSH/systemctl guidance only when the incident explicitly targets one of those names.
- Preserve the exception allowlist when v4 is updated to v5.
- Expose the policy in the UI, downloadable skill, Firestore payload, README, and architecture documentation.
