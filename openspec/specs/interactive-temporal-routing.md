# Interactive temporal routing

A temporal skill can expose exceptions safely when routing is deterministic and downstream of publication replay. The UI must not own the allowlist. Exact identifiers, typed outcomes, negative case tests, and a default-current fallback keep the exception bounded.
