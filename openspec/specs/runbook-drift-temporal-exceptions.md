# Temporal exceptions in Runbook Drift

Migration evidence normally retires old runbooks, but a bounded exception may survive when it is represented as an exact typed allowlist. Replay must validate controller identity, exact-match routing language, retained architecture, and permitted tools. Phrase-order checks are too brittle; validate semantic tokens and structured identity instead.
